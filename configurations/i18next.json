{
  "$schema": "https://json.schemastore.org/eslintrc",
  "extends": ["plugin:i18next/recommended", "plugin:prettier/recommended"],
  "rules": {
    "id-length": ["error", { "exceptions": ["t"] }]
  }
}
